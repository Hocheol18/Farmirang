import React from "react";

const DynamicField = () => {
  const cropSpacing = 1;
  const ridgeSpacing = 2;

  return <div className="w-full h-full bg-yellow-200">내용!!</div>;
};

export default DynamicField;
