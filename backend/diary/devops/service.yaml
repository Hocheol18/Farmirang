#apiVersion: v1
#kind: Service
#metadata:
#  name: app-diary-service
#  namespace: farmirang
#spec:
#  selector:
#    app: app-diary
#  ports:
#    - protocol: TCP
#      port: 8080
#      targetPort: 8080
apiVersion: v1
kind: Service
metadata:
  name: app-diary-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30000
  selector:
    app: app-diary